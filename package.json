{
  "name": "lp-projects-docs",
  "private": true,
  "description": "Pins the Wrangler version used to deploy the built MkDocs site to Cloudflare.",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
